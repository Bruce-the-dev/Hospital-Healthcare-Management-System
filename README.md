# Hospital-Healthcare-Management-System

“A service layer was implemented to introduce in-memory caching using a HashMap, reducing repeated database access. Searching and sorting algorithms were applied on cached patient data. Cache invalidation logic ensures consistency between in-memory data and the database after insert, update, and delete operations.”